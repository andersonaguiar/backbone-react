define(
  'model/car',
  function () {
    'use strict';

    var CarModel = Backbone.Model.extend({
      initialize: function(attrs, opts) { },

      validate: function(attrs, options){ }
    });

    return CarModel;
  }
);

